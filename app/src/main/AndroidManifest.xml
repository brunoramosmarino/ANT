<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bruno.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GastosFijos" />
        <activity android:name=".VerGastosFijos" />
        <activity android:name=".ModificarGastoFijo" />
        <activity android:name=".EliminarGastoFijo" />
        <activity android:name=".VerGastosNoFijos" />
        <activity android:name=".AgregarGastoNoFijo" />
        <activity android:name=".ModificarGastoNoFijo" />
        <activity android:name=".EliminarGastoNoFijo" />
        <activity android:name=".AgregarSueldo" />
        <activity android:name=".Ahorros"></activity>
    </application>

</manifest>